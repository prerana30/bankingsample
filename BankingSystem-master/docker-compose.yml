# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP.NET Core service.

version: '3.4'

services:
  bankingsystem-repo:
    image: bankingsystem-repo
    build:
      context: .
      dockerfile: BankingSystem.API/Dockerfile
    ports:
      - 5000:5000
